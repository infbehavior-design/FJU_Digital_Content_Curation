---
import "../styles/global.css";
---

<section id="lifestory" class="lifestory-container">
  <div class="left fade-in">
    <h2 class="font-bold mb-4 text-4xl md:text-5xl lg:text-6xl">人物故事</h2>
    <h3 class="font-normal mb-4 text-xl md:text-2xl lg:text-3xl">
      －來自兩位咖啡師的自述
    </h3>
    <p class="font-light mb-4 text-base md:text-lg lg:text-xl">
      來自於第一手的採訪，經過二次整理，帶大家看到兩位咖啡師的故事，包括對咖啡的堅持，本人的個性亮點等。
    </p>

    <a href="/lifestory-article" class="more-info">
      了解更多
    </a>

  </div>

  <!-- 右側兩張錯位照片 -->
  <div class="right fade-in">
    <div class="photo-row">
      <!-- 左邊那張 -->
      <img
        src="src/assets/LifeStory1.jpg"
        alt="從電子零件廠留存至今的標語牌"
        class="photo photo-left"
      />
      <!-- 右邊那張 -->
      <img
        src="src/assets/LifeStory2.jpg"
        alt="從電子零件廠留存至今的標語牌"
        class="photo photo-right"
      />
    </div>
  </div>
</section>

<style>
.lifestory-container {
  background-color: #e8c9a8;
  width: 100%;
  height: 100vh; /* Hero 全螢幕 */
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  padding: 0 6vw;
}

/* 左半部文字 */
.left {
  position: relative;
  max-width: 60%;
  padding: 2rem 1rem; /* 讓文字不貼邊 */
}

/* 背後的咖啡豆 */
.left::before {
  content: "";
  position: absolute;
  inset: 0;
  background-image: url("src/assets/coffee3.png"); /* 換成你的圖 */
  background-repeat: no-repeat;
  background-position: left center;
  background-size: 420px auto;

  opacity: 0.3; 
  z-index: 0;
  pointer-events: none;
}

/* 確保文字在上層 */
.left > * {
  position: relative;
  z-index: 1;
}


/* 右側容器 */
.right {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* 新的左右排列容器 */
.photo-row {
  display: flex;
  justify-content: center;
  align-items: flex-start;
  gap: 32px;
}

/* 共同樣式 + 漂浮動畫 */
.photo {
  width: 400px;
  height: 80vh;
  object-fit: cover;
  border-radius: 22px;
  box-shadow: 0 18px 32px rgba(0, 0, 0, 0.15);

  /* 基準位移（之後由 .photo-left / .photo-right 設定） */
  --offset-y: 0px;
  transform: translateY(var(--offset-y));

  /* 漂浮動畫 */
  animation: float 3s ease-in-out infinite alternate;
}

/* 左邊那張：基準往下 20px，再上下小幅漂浮 */
.photo-left {
  --offset-y: 35px;
}

/* 右邊那張：基準往上 20px，再上下小幅漂浮 */
.photo-right {
  --offset-y: -35px;
}

/* 漂浮動畫：以 offset 為中心，上下輕微晃動 */
@keyframes float {
  0% {
    transform: translateY(var(--offset-y));
  }
  100% {
    transform: translateY(calc(var(--offset-y) - 12px));
  }
}


/* 手機版改成上下排列 */
@media (max-width: 768px) {
  .photo-row {
    flex-direction: column;
    gap: 20px;
  }

  .photo {
    width: 100%;
    height: 300px;
    border-radius: 18px;
    transform: none !important; /* 手機版不要錯位 */
  }
}


/* 按鈕樣式維持原本設定 */
.more-info {
  display: inline-block;
  background-color: #6a4b32;
  color: white;
  padding: 12px 28px;
  border: none;
  border-radius: 9999px;
  font-size: 1rem;
  font-weight: 500;
  cursor: pointer;
  transition:
    background-color 0.3s ease,
    transform 0.2s ease,
    box-shadow 0.3s ease;
}

.more-info:hover {
  background-color: #8b6346;
  transform: translateY(-3px);
  box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
}

/* RWD：小螢幕改成上下排，照片不要重疊避免太擠 */
@media (max-width: 768px) {
  .motivation-container {
    grid-template-columns: 1fr;
    grid-auto-rows: auto;
    height: auto;
    padding: 48px 6vw 64px;
    row-gap: 32px;
  }

  .left {
    max-width: 100%;
  }

  .photo-stack {
    max-width: 420px;
    aspect-ratio: 3 / 4;
  }

  .photo {
    position: relative;
    width: 100%;
    height: 48%;
    border-radius: 200px 200px 100px 100px;
  }

  .photo-left {
    top: 0;
    left: 0;
    margin-bottom: 16px;
  }

  .photo-right {
    top: 0;
    right: 0;
    height: 48%;
  }
}
</style>
